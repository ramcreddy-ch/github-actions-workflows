# GitHub Actions Workflows

Reusable workflow templates for CI/CD across the organization.

## Templates
- `maven-build.yml`: Standard Java/Maven CI.
- `docker-publish.yml`: Build and push to GHCR.
- `terraform-plan.yml`: Terraform plan on PR.
